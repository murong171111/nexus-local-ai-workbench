[package]
name = "nexus-ffi"
version = "0.1.0"
edition = "2021"
description = "C ABI JSON bridge for Nexus native shells"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
nexus-core = { path = "../nexus-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
