[package]
name = "nexus-core"
version = "0.1.0"
edition = "2021"
description = "Reusable local workflow core for Nexus"

[dependencies]
rusqlite = { version = "0.40.0", default-features = false, features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
